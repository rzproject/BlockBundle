{% extends sonata_block.templates.block_base %}

{% block block %}
    {% if not menu is defined or not menu is empty %}
        {{ knp_menu_render(menu, menu_options) }}
    {% else %}
        {%- verbatim -%}
        <p>Please select menu to display.
        {%- endverbatim -%}
    {% endif %}
{% endblock %}
